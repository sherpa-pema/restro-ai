{
  "name": "tablecraft-os",
  "private": true,
  "version": "1.0.0",
  "description": "TableCraft OS — AI-Powered Restaurant POS",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite --host\" \"cd backend && uvicorn main:app --port 8000\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "idb": "^8.0.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "concurrently": "^10.0.3",
    "vite": "^6.3.0"
  }
}
